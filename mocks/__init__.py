"""Eight lightweight mock source-system APIs, one FastAPI process.

``mocks.app:app`` mounts them; ``mocks.common`` holds the shared record store and the
dev-only auth constants; ``mocks.systems`` holds one module per system dialect.
"""
